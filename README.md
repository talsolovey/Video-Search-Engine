# Video Search Engine 🔎
**Tech:** Python, Sentence Transformers, FAISS

## Overview
Semantic search system that indexes video metadata and transcripts for fast retrieval.  
Implements TF-IDF baseline and vector embeddings using Sentence Transformers.

## Features
- Vector search with FAISS
- REST API for query interface
- Evaluation module for relevance metrics
- Modular design for custom embedding models

## Quickstart
```bash
git clone https://github.com/talsolovey/video-search-engine
pip install -r requirements.txt
python app.py
